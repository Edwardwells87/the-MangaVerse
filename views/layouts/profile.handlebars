<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bootstrap Dark Mode Page</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.7.2/dist/css/bootstrap.min.css">
  <style>
    body.dark-mode {
      background-color: #212529;
      color: #fff;
    }

    .form-input {
      background-color: #343a40;
      border-color: #343a40;
      color: #fff;
    }

    .blog-list .btn-danger {
      background-color: #dc3545;
      border-color: #dc3545;
    }
  </style>
</head>

<body class="dark-mode">
  <div class="container">
    <div class="row mt-5">
      <div class="col-md-6">
        <h2>Welcome, {{name}}!</h2>
      </div>
    </div>

    <div class="row mt-4">
      <div class="col-md-6">
        <h3>Create a New Blog:</h3>

        <form class="form new-project-form">
          <div class="form-group">
            <label for="project-name">Blog Name:</label>
            <input class="form-input" type="text" id="project-name" name="project-name" />
          </div>
          <div class="form-group">
            <label for="Manga Name">Manga Name:</label>
            <input class="form-input" type="number" id="Manga Name" name="Manga-Name" />
          </div>
          <div class="form-group">
            <label for="Blog-Body">Blog Body:</label>
            <textarea class="form-input" id="Blog-Body" name="Blog-Body"></textarea>
          </div>
          <div class="form-group">
            <button type="submit" class="btn btn-primary">Create</button>
          </div>
        </form>
      </div>

      {{#if Blogs.length}}
      <div class="col-md-6 project-list">
        <h3>Your Blogs:</h3>

        {{#each Blogs as |Blog|}}
        <div class="row mb-2">
          <div class="col-md-8">
            <h4><a href="/project/{{blog.id}}">{{Blog.name}}</a></h4>
          </div>
          <div class="col-md-4">
            <button class="btn btn-sm btn-danger" data-id="{{blog.id}}">DELETE</button>
          </div>
        </div>
        {{/each}}
      </div>
      {{/if}}
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.7.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="./js/profile.js"></script>
</body>

</html>
